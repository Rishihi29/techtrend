name: techtrend_dw
version: "1.0.0"
profile: techtrend_dw

model-paths: ["models"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

vars:
  # lake root is injected by orchestration; default suits `make demo` from repo root
  lake_root: "data/lake"

models:
  techtrend_dw:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
