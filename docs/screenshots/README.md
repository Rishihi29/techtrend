# Screenshots

Capture after `make demo && make serve`:
1. `console-dark.png`  — http://localhost:8000 (default dark theme, full page)
2. `console-drawer.png` — any product card opened (price history + forecast band)
3. `openapi.png`       — http://localhost:8000/docs
4. `grafana.png`       — http://localhost:3000 → TechTrend Platform Health
5. `airflow.png`       — http://localhost:8080 DAG graph of lake_medallion

Then embed them at the top of the root README.
