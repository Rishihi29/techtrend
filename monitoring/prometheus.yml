global:
  scrape_interval: 15s

scrape_configs:
  - job_name: techtrend-api
    static_configs: [{ targets: ["api:8000"] }]
  - job_name: airflow
    static_configs: [{ targets: ["statsd-exporter:9102"] }]
  - job_name: prometheus
    static_configs: [{ targets: ["localhost:9090"] }]
